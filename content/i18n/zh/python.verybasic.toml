# PYTHON LAND — VERY BASIC, in Simplified Chinese. A translation pack (SPEC §12.1):
# the prose of `content/python/verybasic.toml`, quest by quest, and nothing else. The
# question is the brief; the four choices are code and stay English. Code blocks,
# identifiers, sample input and output and the exact strings a program must print
# are the English pack's and are copied verbatim, never translated.

pack   = "python.verybasic"
locale = "zh"

# ---------------------------------------------------------------- 01
[[quest]]
id    = "python.verybasic.01.print"
title = "第一块牌"
story = "宝灵顿道，06:00。当天第一块价格牌还是空的，记号笔在阿美手里。"
brief = '''
程序必须在一行里精确打印 `hello, causewaybay`。有一个内建函数把一行写到标准输出。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
output: hello, causewaybay
```
'''
hints = [
  "print(\"hello, causewaybay\") — 括号，逗号在字符串里面。",
  "Python 3 没有 print 语句；print 是函数。",
]

# ---------------------------------------------------------------- 02
[[quest]]
id    = "python.verybasic.02.int-types"
title = "分账"
story = "两杯咖啡，鱼档柜台上一张钞票。收银机得说每人几杯、还剩多少，它却什么也不说。"
brief = '''
`a` 和 `b` 是整数。程序必须在一行里打印整数商和余数，用一个空格隔开。Python 各有一个运算符。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  7 2
output: 3 1
```
'''
hints = [
  "print(a // b, a % b) — 两个斜杠向下取整除，百分号是余数。",
  "单个斜杠给出浮点数：3.5，不是 3。",
]

# ---------------------------------------------------------------- 03
[[quest]]
id    = "python.verybasic.03.floats"
title = "精确的份额"
story = "市场的秤显示三位小数的克数，收据要两位。收据是空的。"
brief = '''
`x` 和 `y` 是浮点数。程序必须打印 `x / y`，小数点后恰好两位。f-string 在冒号后面带一个格式说明。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  7 2
output: 3.50
```
'''
hints = [
  "print(f\"{x / y:.2f}\") — 冒号后面的 .2f 就是格式。",
  "round() 会丢掉末尾的零：3.5，不是 3.50。",
]

# ---------------------------------------------------------------- 04
[[quest]]
id    = "python.verybasic.04.strings"
title = "菜档招牌"
story = "菜档的招牌是大写漆的，本子上却是小写；写招牌的人要它喊回来。"
brief = '''
`s` 是一行文本。打印它的长度之后，程序必须用大写打印 `s`。有一个字符串方法返回每个字母都大写的副本。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  kwan lee
output: 8
        KWAN LEE
```
'''
hints = [
  "print(s.upper()) — 字符串上的方法，带括号调用。",
  "capitalize() 只把第一个字母变大写。",
]

# ---------------------------------------------------------------- 05
[[quest]]
id    = "python.verybasic.05.bool-if"
title = "单还是双"
story = "宝灵顿道的摊位号一边是双数一边是单数；送货的小子得从号码看出是哪一边。"
brief = '''
`n` 是正整数。`n` 能被二整除时程序必须打印 `even`，否则打印 `odd`。条件是一个把余数和零比较的 `if` 头。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  4
output: even
```
'''
hints = [
  "if n % 2 == 0: — 两个等号是比较；一个是赋值。",
  "n / 2 是浮点除法；对正数 n 它永远不会是零。",
]

# ---------------------------------------------------------------- 06
[[quest]]
id    = "python.verybasic.06.loops"
title = "清点"
story = "早上的袋子数是一加二加三一直加到最后一个摊位，本子上却写着零。"
brief = '''
`n` 是正整数。循环必须访问从 1 到 `n`（含）的每个整数，把每个加进 `total`。`range` 接收起点和终点，终点不包括在内。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  4
output: 10
```
'''
hints = [
  "for i in range(1, n + 1): — 终点不包含，所以是 n + 1。",
  "range(n) 从 0 开始，在 n 之前停下。",
]

# ---------------------------------------------------------------- 07
[[quest]]
id    = "python.verybasic.07.functions"
title = "送货费率"
story = "当天前十单每单付五块，之后每单付三块；骑手的 app 什么也不返回。"
brief = '''
`fare(rides)` 必须返回报酬：前十单每单 5，第十单之后每单 3。函数用一个关键字把值交回去。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  3
output: 15
```
'''
hints = [
  "return min(rides, 10) * 5 + max(rides - 10, 0) * 3",
  "用 print 代替 return 的函数交回的是 None。",
]

# ---------------------------------------------------------------- 08
[[quest]]
id    = "python.verybasic.08.structs"
title = "一个摊位，两个字段"
story = "每个摊位的卡上都有名字和价格。卡的价格字段不见了，标签只打印出半个摊位。"
brief = '''
`Stall.__init__` 接收 `name` 和 `price`。它必须像存 `name` 一样把 `price` 存到对象上，这样 `s.price` 才读得回来。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  tofu 12
output: tofu 12
```
'''
hints = [
  "self.price = price — self 是对象；光秃秃的名字只是局部变量。",
  "两个等号是比较，什么也不存。",
]

# ---------------------------------------------------------------- 09
[[quest]]
id    = "python.verybasic.09.struct-method"
title = "这一行的总额"
story = "价格乘数量是面档每张订单的一行，本该说出它的方法却什么也不说。"
brief = '''
`Stall.total(self)` 必须返回价格乘数量。在方法里面，对象的字段通过 `self` 访问。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  tofu 12 3
output: tofu: 36
```
'''
hints = [
  "return self.price * self.qty — 两个字段都通过 self。",
  "单独的 price 在方法里没有定义。",
]

# ---------------------------------------------------------------- 10
[[quest]]
id    = "python.verybasic.10.enums"
title = "路线颜色"
story = "鱼档上方的港铁图写着三条线；小子手里有那个词，需要它代表的 Enum 成员。"
brief = '''
`word` 是 `island`、`tsuenwan`、`kwuntong` 之一。程序必须把它变成对应的 `Line` 成员。Enum 类可以按成员名索引。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  tsuenwan
output: red
```
'''
hints = [
  "line = Line[word.upper()] — 方括号按名字查成员。",
  "带圆括号的 Line(...) 按值查，而值是数字。",
]

# ---------------------------------------------------------------- 11
[[quest]]
id    = "python.verybasic.11.vector"
title = "早上的清单"
story = "从货车上下来的每个箱子都按来的顺序上早上的清单，清单却一直是空的。"
brief = '''
`xs` 一开始是空的。循环里读到的每个整数 `x` 必须加到末尾，保持输入的顺序。列表有一个方法正是干这个的。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  3 1 4 1
output: 4
        3 1 4 1
```
'''
hints = [
  "xs.append(x) — 一个值，放到末尾。",
  "insert(0, x) 放到最前面，顺序就反了。",
]

# ---------------------------------------------------------------- 12
[[quest]]
id    = "python.verybasic.12.vector-edit"
title = "第一箱走了"
story = "早上清单上的第一箱在念清单之前就卖掉了；它得走，后面的每一个都往前挪一位。"
brief = '''
`xs` 装着这一行的整数。程序必须删掉下标 0 的元素，让其余的往前移。有一个语句按下标删除。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  3 1 4 1
output: 1 4 1
```
'''
hints = [
  "del xs[0] — del 把那个下标的槽位拿走。",
  "remove(0) 找的是值 0，不是下标。",
]

# ---------------------------------------------------------------- 13
[[quest]]
id    = "python.verybasic.13.list"
title = "链子的前端"
story = "送货链挂在柜台上，最新的票挂在最前面；小子却一直挂到后面。"
brief = '''
`chain` 是一个 `deque`。循环里读到的每个 `x` 必须加到最前面，这样最后读到的排在第一。deque 对那一端有一个方法。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  1 2 3
output: 3 2 1
```
'''
hints = [
  "chain.appendleft(x) — 一个词，没有下划线。",
  "append(x) 放到后面。",
]

# ---------------------------------------------------------------- 14
[[quest]]
id    = "python.verybasic.14.maps"
title = "记账本"
story = "一个摊位一本，一样货一行，旁边一个计数；一样货第一次出现时还没有可加的行。"
brief = '''
`counts` 是一个 dict。对每个词 `w`，程序必须给它的计数加一，第一次从零开始。`get` 带默认值读取。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  siu mai har gau siu mai
output: gau 1
        har 1
        mai 2
        siu 2
```
'''
hints = [
  "counts[w] = counts.get(w, 0) + 1 — 带默认值的 get，然后存回去。",
  "对缺失的键 += 1 会抛 KeyError。",
]

# ---------------------------------------------------------------- 15
[[quest]]
id    = "python.verybasic.15.hashtable"
title = "查找"
story = "摊位表把名字映射到号码，问一个不在的摊位应该答 -1，而不是让牌子崩掉。"
brief = '''
`table` 把名字映射到摊位号。对词 `w`，程序必须打印它的号码，`w` 不在表里时打印 `-1`。有一个 dict 方法带默认值读取。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  2
        tofu 3
        fish 7
        egg
output: -1
```
'''
hints = [
  "print(table.get(w, -1)) — 第二个参数是默认值。",
  "w 缺失时 table[w] 抛 KeyError。",
]

# ---------------------------------------------------------------- 16
[[quest]]
id    = "python.verybasic.16.sets"
title = "不同的价格"
story = "六个摊位，四种价格。牌子要的是有几种不同的价格，它却打印六。"
brief = '''
`xs` 装着这一行的整数。程序必须打印有多少个不同的值。集合每种只留一个。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  3 5 3 8 5 3
output: 3
```
'''
hints = [
  "print(len(set(xs))) — set() 去掉重复，len 数个数。",
  "len(xs) 数的是每个值，重复的也算。",
]

# ---------------------------------------------------------------- 17
[[quest]]
id    = "python.verybasic.17.stack"
title = "叠碟"
story = "碟子放到叠子上，从顶上拿；洗碗工却一直从底下拿一个。"
brief = '''
`stack` 是一个当栈用的列表。程序必须把顶上（最后一个）元素取进 `top`。有一个列表方法移除并返回最后一项。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  1 2 3 4
output: 4
        1 2 3
```
'''
hints = [
  "top = stack.pop() — 不带参数就是最后一个。",
  "stack[-1] 读到顶上的，却把它留在叠子上。",
]

# ---------------------------------------------------------------- 18
[[quest]]
id    = "python.verybasic.18.queue"
title = "排队叫号"
story = "号码按拿号的顺序叫；小子却一直叫最新的那个。"
brief = '''
`q` 是一个按顺序装着号码的 `deque`。程序必须把第一个取进 `first`。deque 用一个方法从前端移除。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  1 2 3 4
output: 1
        2 3 4
```
'''
hints = [
  "first = q.popleft() — 一个词。",
  "pop() 从后面拿；q[0] 只读不取。",
]

# ---------------------------------------------------------------- 19
[[quest]]
id    = "python.verybasic.19.heap"
title = "最便宜的先来"
story = "订单乱序进来，厨房每次都要下一个最便宜的；它们堆着的那堆根本不知道哪个最便宜。"
brief = '''
`heap` 一开始是空列表。每个 `x` 必须以这样的方式推入：之后 `heapq.heappop` 先返回最小的。`heapq` 有一个保持堆序的推入。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  5 1 4 2 3
output: 1 2 3 4 5
```
'''
hints = [
  "heapq.heappush(heap, x) — 模块函数，heap 在前。",
  "append() 把 x 放到末尾，破坏堆序。",
]

# ---------------------------------------------------------------- 20
[[quest]]
id    = "python.verybasic.20.sorting"
title = "价格阶梯"
story = "牌子上的价格按摊位开张的顺序排，顾客要最便宜的在前。"
brief = '''
`xs` 装着这一行的整数。程序必须在打印之前把 `xs` 就地升序排好。列表有一个方法干这个。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  3 1 4 1
output: 1 1 3 4
```
'''
hints = [
  "xs.sort() — 带括号，否则什么也不会发生。",
  "sorted(xs, reverse=True) 是降序。",
]

# ---------------------------------------------------------------- 21
[[quest]]
id    = "python.verybasic.21.sort-by-key"
title = "排行榜"
story = "骑手榜是分数从高到低，同分的两个骑手按名字；榜却是到达顺序。"
brief = '''
`rows` 是一个 `(name, score)` 对的列表。它必须按分数降序、再按名字升序排序。`sort` 接收一个返回元组的 `key` 函数。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  3
        mei 7
        alex 9
        bo 7
output: alex 9
        bo 7
        mei 7
```
'''
hints = [
  "rows.sort(key=lambda r: (-r[1], r[0])) — 分数取负就是降序。",
  "key= 是关键字参数；sort 不接受位置上的函数。",
]

# ---------------------------------------------------------------- 22
[[quest]]
id    = "python.verybasic.22.reverse"
title = "从后往前"
story = "舢舨的名字倒着漆在船尾，日志就要它那样。"
brief = '''
`s` 是一行文本。程序必须把它反着打印。步长为 -1 的切片会倒着走一遍字符串。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  kwan lee
output: eel nawk
```
'''
hints = [
  "print(s[::-1]) — 起点终点都空，步长 -1。",
  "reversed(s) 是迭代器；打印它显示的是地址。",
]

# ---------------------------------------------------------------- 23
[[quest]]
id    = "python.verybasic.23.closures"
title = "加价"
story = "水果档给每个批发价加同样的加价，本该记住加价的函数却忘了它。"
brief = '''
`make_adder(k)` 定义了一个内部函数 `add`，返回 `x + k`。它必须把那个内部函数本身交回去，这样之后调用 `add(x)` 时 `k` 还记得。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  5
        1 2 3
output: 6 7 8
```
'''
hints = [
  "return add — 光秃秃的名字，不带括号。",
  "add() 是不带参数地调用它，会失败。",
]

# ---------------------------------------------------------------- 24
[[quest]]
id    = "python.verybasic.24.lambdas"
title = "偶数的平方"
story = "双数摊位按摊号的平方付费，单数摊位不付；账本要一个总数。"
brief = '''
`xs` 装着这一行的整数。`total` 必须是偶数值的平方之和，用 `map`、`filter` 和两个 lambda 搭出来。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  1 2 3 4 5 6
output: 56
```
'''
hints = [
  "total = sum(map(lambda x: x * x, filter(lambda x: x % 2 == 0, xs)))",
  "filter 留下 lambda 判为 True 的；map 做变换。",
]

# ---------------------------------------------------------------- 25
[[quest]]
id    = "python.verybasic.25.threads"
title = "十二个摊位一起"
story = "每个摊位在自己的线程里给库存数平方，订货单却在任何一个完成之前就打印了。"
brief = '''
`threads` 里的每个线程都已启动。在打印结果之前，主线程必须等待每一个结束。线程有一个方法干这个。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  1 2 3
output: 1 4 9
        14
```
'''
hints = [
  "t.join() — 阻塞到那个线程完成。",
  "第二次 start() 会抛 RuntimeError。",
]

# ---------------------------------------------------------------- 26
[[quest]]
id    = "python.verybasic.26.mutex"
title = "一个计数器，四只手"
story = "四个收银员同时往一个计数器上加；计数器有一把锁，写之前却没人拿。"
brief = '''
`lock` 是一个 `threading.Lock`。在 `counter += 1` 之前，每个 worker 必须拿到锁；写之后的释放已经写好了。有一个方法获取它。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  1000
output: 4000
```
'''
hints = [
  "lock.acquire() — 然后写，然后是已经在那里的释放。",
  "释放一把没人持有的锁会抛 RuntimeError。",
]

# ---------------------------------------------------------------- 27
[[quest]]
id    = "python.verybasic.27.binary-tree"
title = "None"
story = "05:59 的价格牌：价格树的左枝是 None，那一侧的第一次插入掉在了地上。"
brief = '''
`insert(n, v)` 返回装入 `v` 之后的树根；空的情况和右边都写好了。对较小的 `v`，左子节点必须被替换成往它里面插入的结果，这样 `None` 子节点就变成了节点。

选出能做到这一点的那一行，然后把它敲在 `# FILL` 注释所在的位置。

```
input:  5 3 8 1 4
output: 1 3 4 5 8
        3
```
'''
hints = [
  "n.left = insert(n.left, v) — 递归返回子树的新根；存起来。",
  "调用 insert 而不存结果，None 还留在原处。",
]
