# A parse error: the colon after the signature never arrives. py_compile
# reports it before a single line runs — the "compile" phase of a language
# that otherwise has none.
def main()
    print("hello, causewaybay")


main()
