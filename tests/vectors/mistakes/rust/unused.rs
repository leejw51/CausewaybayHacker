// unused_variables + unused_imports. Both are WARNINGS: this file compiles
// and runs. The classifier has to read the warning stream, not only errors.
use std::collections::HashMap;

fn main() {
    let leftover = 7;
    println!("hello, causewaybay");
}
