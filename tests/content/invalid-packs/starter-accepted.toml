# SPEC §9.5: every starter must be REJECTED. This one already solves the
# quest, so importing it would hand the player a cleared node.
pack = "rust.basic"
land = "rust"
category = "basic"
version = 1

[[quest]]
id = "rust.basic.01.freebie"
node = 1
title = "FREEBIE"
difficulty = 1
concepts = ["io"]
requires = []
map = { x = 0.1, y = 0.1, kind = "quest" }
brief = "Print it."
starter = '''fn main() {
    println!("a");
}
'''
solution = '''fn main() {
    println!("a");
}
'''
[quest.tests]
harness = "stdio"
timeout_ms = 5000
match = "trim"
cases = [ { name = "a", stdin = "", expect = "a\n", visible = true } ]
